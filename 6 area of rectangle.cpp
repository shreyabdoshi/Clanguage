#include<stdio.h>
int main()
{
	int l,w;
	printf("Enter the value of Length:");
	scanf("%d",&l);
	printf("Enter the value of width:");
	scanf("%d",&w);
	printf("Area of Rectangle is: %d",l*w);
	return 0;	
}
